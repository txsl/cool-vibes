# Development/test dependencies (not needed to run the server).
# Install with: pip3 install -r requirements-dev.txt

pytest>=8.0
httpx>=0.27          # required by FastAPI TestClient for endpoint tests
